//import { createStore } from "redux";
//import cakeReducer from "./cakeshop/CakeReducer";

//const store = createStore(cakeReducer);

//export default store;
import { createStore } from "redux";
import icecreamReducer from "./";

const store = createStore(icecreamReducer);

export default store;
